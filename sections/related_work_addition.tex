\noindent\textit{Deformation-graph correction.}
Kimera-PGMO~\cite{kimera_pgmo} propagates pose- and deformation-graph updates to mesh vertices through spatial and temporal interpolation. We evaluate this graph-based correction separately on a native-mesh subset because it jointly changes trajectory and mesh, unlike the mapping baselines in our common-trajectory benchmark.
